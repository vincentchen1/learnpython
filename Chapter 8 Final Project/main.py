# append to an empty dictionary?
# while loop
# somehow include while loop in list to ask for more than 1 pokemon
# display all user input, not just the most recent set

from trainer import create_trainer

create_trainer()
